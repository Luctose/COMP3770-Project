using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ArcherDriver : MonoBehaviour
{
    Character entity;
    // Start is called before the first frame update
    void Start()
    {
        entity = new Archer();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
