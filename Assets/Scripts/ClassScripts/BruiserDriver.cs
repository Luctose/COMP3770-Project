using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BruiserDriver : MonoBehaviour
{
    Character entity;
    // Start is called before the first frame update
    void Start()
    {
        entity = new Bruiser();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
