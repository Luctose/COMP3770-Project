using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SwordsmanDriver : MonoBehaviour
{
    Character entity;
    // Start is called before the first frame update
    void Start()
    {
        entity = new Swordsman();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
